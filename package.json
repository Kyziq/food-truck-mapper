{
  "name": "food-truck-mapper",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"bun run dev:backend\" \"bun run dev:mobile\"",
    "dev:backend": "cd apps/backend && bun src/server.ts",
    "dev:mobile": "cd apps/mobile && flutter run"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.1.18",
  "workspaces": [
    "apps/*"
  ]
}
